# Copy to .env (git-ignored) and fill in. Never commit real values.
# TUNNEL_ID goes into the profile via `tunnel-client init --tunnel-id`. API_KEY is passed as CONTROL_PLANE_API_KEY at run time.
TUNNEL_ID=
API_KEY=
